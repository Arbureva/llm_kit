name: llm_kit_example
description: A runnable Flutter chat page to test llm_kit.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.6.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter

  # 引用上一级目录里的 llm_kit 包（本地路径依赖）。
  llm_kit:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
